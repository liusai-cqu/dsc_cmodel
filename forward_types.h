#ifndef FORWARD_TYPES_H
#define FORWARD_TYPES_H

// 前向声明content_t
typedef void* content_t;

// 其他可能需要的前向声明
typedef struct _vcs_dpi_dsc_cfg_t dsc_cfg_t;
typedef struct _vcs_dpi_pic_t pic_t;

#endif /* FORWARD_TYPES_H */